/// The current version of the flutter_cleanup CLI.
///
/// Declared as a compile-time constant so it is available in compiled
/// binaries without reading `pubspec.yaml` at runtime. Keep this in sync
/// with the `version` field in `pubspec.yaml`.
const String packageVersion = '1.0.0';
