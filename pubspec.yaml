name: flutter_cleanup
description: A CLI tool to identify unused files, assets, dead code, and dependency relationships in Flutter projects.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.12.0

# Add regular dependencies here.
dependencies:
  args: ^2.5.0
  glob: ^2.1.2
  path: ^1.9.0
  yaml: ^3.1.2

dev_dependencies:
  analyzer: ^13.1.0
  lints: ^6.0.0
  test: ^1.25.6
